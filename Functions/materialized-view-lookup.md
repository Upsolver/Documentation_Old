# Materialized View Lookup / Join Streams

Queries on [Upsolver Materialized Views](/MaterializedViews/README.md) have very low latency \(milliseconds\). As a result, it's possible to query materialized views for every event, thus creating a join between streams.

